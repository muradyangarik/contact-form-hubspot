<?xml version="1.0"?>
<ruleset name="Contact Form HubSpot">
    <description>PHP_CodeSniffer configuration for Contact Form HubSpot plugin</description>
    
    <!-- What to scan -->
    <file>.</file>
    
    <!-- Exclude patterns -->
    <exclude-pattern>*/vendor/*</exclude-pattern>
    <exclude-pattern>*/node_modules/*</exclude-pattern>
    <exclude-pattern>*/tests/*</exclude-pattern>
    <exclude-pattern>*/languages/*</exclude-pattern>
    <exclude-pattern>*.min.js</exclude-pattern>
    <exclude-pattern>*.min.css</exclude-pattern>
    
    <!-- How to scan -->
    <arg name="colors"/>
    <arg name="extensions" value="php"/>
    <arg value="np"/>
    
    <!-- Rules: Check PHP version compatibility -->
    <config name="testVersion" value="7.4-"/>
    
    <!-- Rules: WordPress Coding Standards -->
    <rule ref="WordPress"/>
    <rule ref="WordPress-Extra"/>
    <rule ref="WordPress-Docs"/>
    
    <!-- Customize some rules -->
    <rule ref="WordPress.Files.FileName">
        <exclude name="WordPress.Files.FileName.NotHyphenatedLowercase"/>
    </rule>
    
    <rule ref="WordPress.NamingConventions.ValidFunctionName">
        <exclude name="WordPress.NamingConventions.ValidFunctionName.MethodNameInvalid"/>
    </rule>
    
    <rule ref="WordPress.WhiteSpace.ControlStructureSpacing">
        <properties>
            <property name="blank_line_check" value="true"/>
        </properties>
    </rule>
    
    <!-- Ignore some rules for test files -->
    <rule ref="WordPress.PHP.DevelopmentFunctions">
        <exclude name="WordPress.PHP.DevelopmentFunctions.error_log"/>
    </rule>
</ruleset>



